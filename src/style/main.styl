@font-face 
    font-family ColtherBold 
    src url(../fonts/TRY_Clother-Bold.ttf)

@font-face 
    font-family ColtherRegular 
    src url(../fonts/TRY_Clother-Regular.ttf)

@font-face 
    font-family ColtherLight 
    src url(../fonts/TRY_Clother-Light.ttf)

html
body
    margin 0
    padding 0
    width 100%
    height 100%
    overflow hidden
    font-family ColtherRegular
    scroll-behavior smooth

canvas
    width 100%
    z-index 5

.preloader
    width 100vw
    height 100vh
    position absolute
    color white
    top 0
    display flex
    align-items center
    justify-content center
    font-size 3em
    .point__first
        animation test 2s infinite
    .point__second
        animation test 2s infinite
        animation-delay .66666s
    .point__three
        animation test 2s infinite
        animation-delay 1.333s

.loader
    display flex
    width 100vw
    height 100vh
    background #000
    color white
    flex-direction column
    justify-content center
    align-items center
    span
        padding 5% 0
        font-size 2em
    .loader__bar
        width 60%
        height 1%
        background #fff
        // animation loadAnimation 5s
        transform scaleX(0)
        transform-origin 0.0
        transition .1s

header
    display block
    width 100vw
    position fixed
    border-bottom 1px solid #fff
    background #000
    z-index: 10
    nav
        width 90%
        margin 0 auto
        // padding 0 5%
        display flex
        justify-content space-between
        align-items center
        font-family ColtherRegular
        color #FFF
        a
            color #fff
            text-decoration none
        button
            position relative
            -webkit-appearance none
            background none
            font-size 1em
            border none
            color #fff
            cursor pointer
            &:after
                width 0
                animation underlineAnimationNone .5s
                transform-origin 1
            &:hover:after
                content ' '
                position absolute
                width 120%
                height .2vw
                top 100%
                left -10%
                background #fff
                transform-origin 0.0
                animation underlineAnimation .5s
            &:focus
                outline none
        h1
            font-family ColtherBold
            span
                font-family ColtherBold
                color #000000
                -webkit-text-stroke 1px #fff
        ul
            display flex
            justify-content space-between
            align-items center
            list-style none
            li
                padding 20px
                &:nth-child(4)
                    padding-right 0

main
    display block
    z-index 1
    section
        width 100vw
        // height 100vh

.instruction
    height 100vh
    position absolute
    background #00000090
    display flex
    align-items center
    justify-content center
    z-index 20
    .instruction__content
        color #fff
        background #000
        border 1px solid #fff
        padding 5% 10%
        // animation test .7s
        // animation-delay 5s
        h2
            text-align center
            margin-top 0
        table
            width max-content
            padding 10% 0
            margin 0 auto
            tbody
                width 100%
                display flex
                flex-direction column
                tr
                    padding 5% 0
                    th
                        div
                            width 3vw
                            height 3vw
                            display flex
                            justify-content center
                            align-items center
                            border 1px solid #fff
                    td
                        width 100%
                        padding-left 20%
        button
            width 100%
            padding 5%
            background #fff
            color #000
            font-size 1em
            -webkit-appearance none
            transition .3s
            cursor pointer
            &:focus
                outline none
            &:hover
                background #000
                color #fff
                transition .3s

.projects
    display none
    position absolute
    background transparent
    top 0
    animation desactivate .7s
    article 
        width 100vw
        height 100vh
        position relative
        display flex
        align-items center
        justify-content center
        color #fff
        background #000
        &:first-child
            .projects__button
                justify-content flex-end
        .projects__button
            width 60%
            margin 0 auto
            position absolute
            bottom 10%
            display flex
            justify-content space-between
            a
                color #fff
                padding 20px 30px
                border 1px solid #fff
                text-decoration none
                transition .3s
                &:hover
                    background #fff
                    color #000
        .projects__content
            width 90%
            display flex
            align-items center
            justify-content space-between
            .content__left
                width 70%
                display flex
                align-items center
                img
                    width 100%
                    height 100%
            .content__bar
                width .5%
                height 50vh
                background #fff
                margin 0 5%
            .content__right
                width 70%
                display flex
                flex-direction column
                justify-content space-between
                h1
                    margin-top 0
                    margin-bottom 1%
                    font-family ColtherBold
                    font-size 2em
                small
                    margin-bottom 2%
                    font-family ColtherLight
                p
                    font-family ColtherRegular
                a
                    width max-content
                    color #fff
                    text-decoration none
                    padding 15px
                    border 1px solid #fff
                    transition .3s
                    &:hover
                        background #fff
                        color #000

.about
    width 100vw
    display none
    position absolute
    background #000
    .about__content
        width 90%
        height 100vh
        margin 0 auto
        background #000
        display flex
        align-items center
        justify-content space-between
        color #fff
        .content__left
            width 20vw
            height 20vw
            border 1px solid #FFF
            border-radius 50%
            overflow hidden
            img
                width 100%
        .content__bar
            width 20vw
            height .1vh
            background #fff
        .content__right
            text-align center
            width 30vw
.contact
    height 100vh
    display none
    align-items center
    justify-content center
    background #000
    color #fff
    position absolute
    top 0
    .contact__content
        width 90%
        display flex
        // justify-content space-between
        margin-top 5%
        .content__left
            width 100%
            padding-right 5%
            p
                margin-block-start auto
        .content__right
            width 100%
            padding-left 5%
            input
                width -webkit-fill-available
                padding 2%
                font-size 1em
                background #000
                border 1px solid #fff
                color #fff
                &:focus
                    outline none
            textarea
                width -webkit-fill-available
                padding 2%
                font-size 1em
                background #000
                border 1px solid #fff
                color #fff
                resize none
                &:focus
                    outline none
            button
                width 100%
                padding 2%
                margin-top 5%
                font-size 1em
                color #000
                background #fff
                text-transform uppercase
                transition .5s
                -webkit-appearance none
                cursor pointer
                &:hover
                    background #000
                    color #fff
                    transition .5s
            h2
                margin-bottom 2%
                &:nth-child(1)
                    margin-block-start auto

footer
    position fixed
    bottom 0
    background #000
    color #fff
    width 100vw
    display flex
    justify-content space-evenly
    font-size 1em
    border-top 1px solid #fff
    padding-top 5px
    p
        margin-block-end 0
        margin-block-start 0
    a
        color #fff
        transition .3s
        &:hover
            color #8e8e8e

.message
    padding 10px 15px
    margin 10px

.message.error
    background pink


.message.success
    background greenyellow

.hamburger
    display none

.header__overlay
    display none

.active
    display block
    animation opacity .7s

.desactive
    animation desactivate .7s

@keyframes opacity {
    from {opacity: 0}
    to {opacity: 1}
    // from {transform: translateX(-100vw)}
    // to {transform: translateX(0)}
    }

@keyframes desactivate {
    from {opacity: 1}
    to {opacity: 0}
    // from {transform: translateX(-100vw)}
    // to {transform: translateX(0)}
    }

@keyframes underlineAnimation{
    from {transform: scaleX(0)}
    to {transform: scaleX(1)}
    }
@keyframes underlineAnimationNone{
    from {transform: scaleX(1)}
    to {transform: scaleX(0)}
    }

@keyframes test{
    0%,100% {opacity: 0}
    60% {opacity: 1}
    }

// @keyframes loadAnimation{
//     0% {transform: scaleX(0)}
//     60% {transform: scaleX(.3)}
//     90% {transform: scaleX(.35)}
//     100% {transform: scaleX(1)}
//     }

// @media (max-width: 500px)
//   .

.mobile
    display none
    width 100vw
    height 100vh
    position absolute
    top 0
    color white
    background #000
    justify-content center
    align-items center
    font-size 2em
    text-align center
    z-index 10000


/* Hamburger */
@media (max-width: 700px)
    .mobile
        display flex
    .nav__right ul
        display none
    .contact
        overflow scroll
        .contact__content
            flex-direction column
            margin-top 70% !important
            .content__right
                padding-left 0
            .content__left
                padding-right 0
    .projects
        article 
            padding-top 20vh
            height 70vh
            flex-direction column
            overflow scroll
            .projects__content
                flex-direction column
                .content__bar
                    width 75.5% !important
                    height .1vh !important
                    margin 5% !important

    .hamburger {
            padding: 15px 15px;
            display: inline-block;
            cursor: pointer;
            transition-property: opacity, filter;
            transition-duration: 0.15s;
            transition-timing-function: linear;
            font: inherit;
            color: inherit;
            text-transform: none;
            background-color: transparent;
            border: 0;
            margin: 0;
            overflow: visible; }
            .hamburger:hover {
            opacity: 0.7; }
            .hamburger.is-active:hover {
            opacity: 0.7; }
            .hamburger.is-active .hamburger-inner,
            .hamburger.is-active .hamburger-inner::before,
            .hamburger.is-active .hamburger-inner::after {
            background-color: #fff; }
        
        .hamburger-box {
            width: 40px;
            height: 24px;
            display: inline-block;
            position: relative; }
        
        .hamburger-inner {
            display: block;
            top: 50%;
            margin-top: -2px; }
            .hamburger-inner, .hamburger-inner::before, .hamburger-inner::after {
            width: 40px;
            height: 4px;
            background-color: #fff;
            border-radius: 4px;
            position: absolute;
            transition-property: transform;
            transition-duration: 0.15s;
            transition-timing-function: ease; }
            .hamburger-inner::before, .hamburger-inner::after {
            content: "";
            display: block; }
            .hamburger-inner::before {
            top: -10px; }
            .hamburger-inner::after {
            bottom: -10px; }
        
            .hamburger--3dy .hamburger-box {
                perspective: 80px; }
            
            .hamburger--3dy .hamburger-inner {
                transition: transform 0.15s cubic-bezier(0.645, 0.045, 0.355, 1), background-color 0s 0.1s cubic-bezier(0.645, 0.045, 0.355, 1); }
                .hamburger--3dy .hamburger-inner::before, .hamburger--3dy .hamburger-inner::after {
                transition: transform 0s 0.1s cubic-bezier(0.645, 0.045, 0.355, 1); }
            
            .hamburger--3dy.is-active .hamburger-inner {
                background-color: transparent !important;
                transform: rotateX(-180deg); }
                .hamburger--3dy.is-active .hamburger-inner::before {
                transform: translate3d(0, 10px, 0) rotate(45deg); }
                .hamburger--3dy.is-active .hamburger-inner::after {
                transform: translate3d(0, -10px, 0) rotate(-45deg); }

    .header__overlay
        width: 100%;
        background: #000;
    
    .overlay__content 
        width 100%
        margin -18px 0
        ul
            padding-left 0
            list-style none
            display flex
            flex-direction column
            li
                width 100%
                text-align center
                display block
                border-bottom 1px solid #fff
                &:last-child
                    border-bottom none
                button
                    width 100%
                    height 100px
                    position relative
                    -webkit-appearance none
                    background none
                    font-size 1em
                    border none
                    color #fff
                    &:focus
                        outline none

    .overlay-on
        display: flex !important;
        position: absolute;
        transition: .5s;

